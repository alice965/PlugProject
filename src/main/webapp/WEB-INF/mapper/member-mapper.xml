<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
						"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="member">
	<insert id="addBasic">
		insert into member(id, nickname, pass) values (#{id},#{nickname}, #{pass})		
	</insert>
	<insert id="addDetail">
		insert into detail(id) values (#{id})		
	</insert>
	
	<select id="checkByIdAndPass" resultType="java.lang.Integer">
		select count(*) from member where (id=#{id})
			 and pass=#{pass, jdbcType=VARCHAR }
	</select>
	
	<select id="readOneById" resultType="java.util.HashMap">
		select * from member where (id=#{id})
	</select>
	
	<insert id="addProfile">
		insert into detail values(profile_seq.nextval, #{id}, #{url}, regdate)
	</insert>
	
	<select id="readLatestProfileById" resultType="java.util.HashMap">
		select * from (select * from detail where id=#{id} order by regdate desc) where rownum =1
	</select>
</mapper>













