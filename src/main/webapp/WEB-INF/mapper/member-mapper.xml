<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
						"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="member">
	<insert id="addBasic">
		insert into member(id, nickname, pass) values (#{id},#{nickname}, #{pass})		
	</insert>
	<insert id="addDetail">
		insert into detail(id) values (#{id})		
	</insert>
	<select id="checkById" resultType="java.lang.Integer"> <!-- 1이상이면 같은 아이디가 있는 것. -->
		select count(*) from member where (id=#{id})
	</select>
	<select id="checkByNickname" resultType="java.lang.Integer"> <!-- 1이상이면 같은 아이디가 있는 것. -->
		select count(*) from member where (nickname=#{nickname})
	</select>
	
</mapper>













