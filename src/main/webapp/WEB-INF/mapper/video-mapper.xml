<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
						"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="video">
	<insert id="addVideo">
		insert into videolist(num, video_num, add_id, video_title, video_id, channel_url, adddate) 
		values (#{num}, video_seq.nextval, #{add_id}, #{video_title}, #{video_id}, #{channel_url}, sysdate)	
	</insert>
	
	<select id="selectVideoList"  parameterType="java.lang.Integer" resultType="java.util.HashMap">
		select * from videolist where (num=#{num})
	</select>
	
</mapper>












